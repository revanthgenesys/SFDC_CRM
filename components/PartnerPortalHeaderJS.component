<apex:component >
    <script>
        var varTimeout = setInterval(hideSearchButton, 0);          
        function hideSearchButton(){      
           if(document.getElementsByClassName('headerSearchContainer') != null){
               document.getElementsByClassName('headerSearchContainer')[0].style.display = 'none';
               clearInterval(varTimeout);
           }
        }    
    </script>
</apex:component>